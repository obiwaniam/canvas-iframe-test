// Companion reporting config that lives with the page.
// Point these to your server endpoints (same host recommended).
window.MAINTENANCE_REPORT_ENDPOINT = '';
window.MAINTENANCE_RESULTS_ENDPOINT = '';
